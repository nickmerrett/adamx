# Requirements for PDF to MAD conversion tool

# Core dependencies
docling>=2.0.0              # IBM's PDF conversion toolkit
sentence-transformers>=2.2.2  # For generating vector embeddings
spacy>=3.7.0               # For NLP and entity extraction

# Optional but recommended spaCy models
# Install with: python -m spacy download en_core_web_sm
# python -m spacy download en_core_web_lg  # Larger, more accurate model

# Additional utility dependencies
numpy>=1.21.0
torch>=1.9.0              # Required by sentence-transformers
transformers>=4.21.0      # Required by sentence-transformers
tqdm>=4.64.0              # Progress bars
requests>=2.28.0          # For downloading models/data

# Optional dependencies for enhanced functionality
# opencv-python>=4.5.0     # For advanced image processing
# pdf2image>=1.16.0        # Alternative PDF to image conversion
# pytesseract>=0.3.10      # OCR capabilities
# langdetect>=1.0.9        # Language detection